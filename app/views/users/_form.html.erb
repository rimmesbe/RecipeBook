<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions user-form">
    <%= f.label :name, "UserName:", class: "user-info-label" %>
    <%= f.text_field :name %>
    <%= f.label :password, "Password:", class: "user-info-label" %>
    <%= f.password_field :password %><br><br>
    <%= f.submit class: "submit-user-update" %>
  </div>
<% end %>
